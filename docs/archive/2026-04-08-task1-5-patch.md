# Tasks 1-5 缺口补丁计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 Tasks 1-5 实现中发现的 3 个缺口：缺失的 UpdateProvisionNetwork API、oobPassword 缺 @EncryptColumn、ChangeState Maintaining vs Maintenance 枚举不一致

**Architecture:** 遵循已有的 ZStack API 模式（参照 APIUpdateServerPoolMsg），不引入新架构

**Tech Stack:** Java 8, Spring 5.2.25, Hibernate 5.3.26, Groovy (tests)

**Build Commands:**
```bash
# 单模块编译
JAVA_HOME=/usr/lib/jvm/java-8-openjdk-amd64 mvn clean install -pl plugin/physicalServer -am -DskipTests -Dmaven.repo.local=.m2/repository

# Premium + SDK
JAVA_HOME=/usr/lib/jvm/java-8-openjdk-amd64 mvn -Dmaven.test.skip=true -P premium clean install
JAVA_HOME=/usr/lib/jvm/java-8-openjdk-amd64 ./runMavenProfile sdk
JAVA_HOME=/usr/lib/jvm/java-8-openjdk-amd64 ./runMavenProfile apihelper
```

**Branch:** `feature/unifi-host-dev`

---

## 缺口分析

| # | 缺口 | 严重度 | 说明 |
|---|---|---|---|
| 1 | 缺 APIUpdateProvisionNetworkMsg | **高** | ProvisionNetwork 没有 Update API，无法修改 name/description/DHCP 字段 |
| 2 | oobPassword 缺 @EncryptColumn | **中** | 明文存储密码，安全风险。已在 Task 9 计划中，但只需一行注解可以提前修复 |
| 3 | ChangeState 返回 "Maintenance" 但测试期望 "Maintaining" | **低** | PhysicalServerState 枚举值是 `Maintenance`，测试写的 `Maintaining`，需对齐 |

**注意:** FR-006 的 Status/PowerStatus 自动更新已在 Task 9 计划中覆盖，不在此补丁范围。

---

### Task P1: 添加 APIUpdateProvisionNetworkMsg

**Goal:** 补齐 ProvisionNetwork CRUD 中缺失的 Update API，对齐 ServerPool 和 PhysicalServer 的 CRUD 完整性。

**Files to create/modify:**
- **新建:** `header/src/main/java/org/zstack/header/server/APIUpdateProvisionNetworkMsg.java`
- **新建:** `header/src/main/java/org/zstack/header/server/APIUpdateProvisionNetworkEvent.java`
- **修改:** `plugin/physicalServer/src/main/java/org/zstack/server/PhysicalServerManagerImpl.java` — 添加 handle 方法 + 路由
- **修改:** `conf/serviceConfig/provisionNetwork.xml` — 添加消息路由
- **修改:** `test/src/test/groovy/org/zstack/test/integration/server/ServerPoolCrudCase.groovy` — 添加测试

#### Steps

- [ ] **Step 1.1: 创建 APIUpdateProvisionNetworkMsg.java**

  ```java
  // header/src/main/java/org/zstack/header/server/APIUpdateProvisionNetworkMsg.java
  package org.zstack.header.server;

  import org.springframework.http.HttpMethod;
  import org.zstack.header.identity.Action;
  import org.zstack.header.message.APIMessage;
  import org.zstack.header.message.APIParam;
  import org.zstack.header.rest.RestRequest;

  @Action(category = PhysicalServerConstant.ACTION_CATEGORY)
  @RestRequest(path = "/provision-networks/{uuid}/actions", method = HttpMethod.PUT, isAction = true, responseClass = APIUpdateProvisionNetworkEvent.class)
  public class APIUpdateProvisionNetworkMsg extends APIMessage {
      @APIParam(resourceType = PhysicalServerProvisionNetworkVO.class, checkAccount = true)
      private String uuid;

      @APIParam(required = false, maxLength = 255)
      private String name;

      @APIParam(required = false, maxLength = 2048)
      private String description;

      @APIParam(required = false, maxLength = 128)
      private String dhcpInterface;

      @APIParam(required = false, maxLength = 64)
      private String dhcpRangeStartIp;

      @APIParam(required = false, maxLength = 64)
      private String dhcpRangeEndIp;

      @APIParam(required = false, maxLength = 64)
      private String dhcpRangeNetmask;

      @APIParam(required = false, maxLength = 64)
      private String dhcpRangeGateway;

      public String getUuid() { return uuid; }
      public void setUuid(String uuid) { this.uuid = uuid; }

      public String getName() { return name; }
      public void setName(String name) { this.name = name; }

      public String getDescription() { return description; }
      public void setDescription(String description) { this.description = description; }

      public String getDhcpInterface() { return dhcpInterface; }
      public void setDhcpInterface(String dhcpInterface) { this.dhcpInterface = dhcpInterface; }

      public String getDhcpRangeStartIp() { return dhcpRangeStartIp; }
      public void setDhcpRangeStartIp(String dhcpRangeStartIp) { this.dhcpRangeStartIp = dhcpRangeStartIp; }

      public String getDhcpRangeEndIp() { return dhcpRangeEndIp; }
      public void setDhcpRangeEndIp(String dhcpRangeEndIp) { this.dhcpRangeEndIp = dhcpRangeEndIp; }

      public String getDhcpRangeNetmask() { return dhcpRangeNetmask; }
      public void setDhcpRangeNetmask(String dhcpRangeNetmask) { this.dhcpRangeNetmask = dhcpRangeNetmask; }

      public String getDhcpRangeGateway() { return dhcpRangeGateway; }
      public void setDhcpRangeGateway(String dhcpRangeGateway) { this.dhcpRangeGateway = dhcpRangeGateway; }

      public static APIUpdateProvisionNetworkMsg __example__() {
          APIUpdateProvisionNetworkMsg msg = new APIUpdateProvisionNetworkMsg();
          msg.setUuid(uuid());
          msg.setName("updated-pxe-net");
          msg.setDescription("updated description");
          return msg;
      }
  }
  ```

- [ ] **Step 1.2: 创建 APIUpdateProvisionNetworkEvent.java**

  ```java
  // header/src/main/java/org/zstack/header/server/APIUpdateProvisionNetworkEvent.java
  package org.zstack.header.server;

  import org.zstack.header.message.APIEvent;
  import org.zstack.header.rest.RestResponse;

  @RestResponse(allTo = "inventory")
  public class APIUpdateProvisionNetworkEvent extends APIEvent {
      private PhysicalServerProvisionNetworkInventory inventory;

      public APIUpdateProvisionNetworkEvent() {}

      public APIUpdateProvisionNetworkEvent(String apiId) {
          super(apiId);
      }

      public PhysicalServerProvisionNetworkInventory getInventory() { return inventory; }
      public void setInventory(PhysicalServerProvisionNetworkInventory inventory) { this.inventory = inventory; }

      public static APIUpdateProvisionNetworkEvent __example__() {
          APIUpdateProvisionNetworkEvent evt = new APIUpdateProvisionNetworkEvent();
          return evt;
      }
  }
  ```

- [ ] **Step 1.3: 在 PhysicalServerManagerImpl 中添加 handler 和路由**

  在 `handleApiMessage()` 的 ProvisionNetwork 分支中，在 `APIDeleteProvisionNetworkMsg` 之后添加:

  ```java
  } else if (msg instanceof APIUpdateProvisionNetworkMsg) {
      handle((APIUpdateProvisionNetworkMsg) msg);
  ```

  在 ProvisionNetwork handlers 区域添加方法:

  ```java
  private void handle(APIUpdateProvisionNetworkMsg msg) {
      PhysicalServerProvisionNetworkVO vo = dbf.findByUuid(msg.getUuid(), PhysicalServerProvisionNetworkVO.class);
      if (vo == null) {
          throw new OperationFailureException(operr("ProvisionNetwork[uuid:%s] not found", msg.getUuid()));
      }
      boolean update = false;
      if (msg.getName() != null) { vo.setName(msg.getName()); update = true; }
      if (msg.getDescription() != null) { vo.setDescription(msg.getDescription()); update = true; }
      if (msg.getDhcpInterface() != null) { vo.setDhcpInterface(msg.getDhcpInterface()); update = true; }
      if (msg.getDhcpRangeStartIp() != null) { vo.setDhcpRangeStartIp(msg.getDhcpRangeStartIp()); update = true; }
      if (msg.getDhcpRangeEndIp() != null) { vo.setDhcpRangeEndIp(msg.getDhcpRangeEndIp()); update = true; }
      if (msg.getDhcpRangeNetmask() != null) { vo.setDhcpRangeNetmask(msg.getDhcpRangeNetmask()); update = true; }
      if (msg.getDhcpRangeGateway() != null) { vo.setDhcpRangeGateway(msg.getDhcpRangeGateway()); update = true; }
      if (update) {
          vo = dbf.updateAndRefresh(vo);
      }

      APIUpdateProvisionNetworkEvent evt = new APIUpdateProvisionNetworkEvent(msg.getId());
      evt.setInventory(PhysicalServerProvisionNetworkInventory.valueOf(vo));
      bus.publish(evt);
  }
  ```

- [ ] **Step 1.4: 在 provisionNetwork.xml 中添加消息路由**

  在 `APIDeleteProvisionNetworkMsg` 后添加:

  ```xml
  <message>
      <name>org.zstack.header.server.APIUpdateProvisionNetworkMsg</name>
  </message>
  ```

- [ ] **Step 1.5: 在 ServerPoolCrudCase.groovy 添加测试**

  在 `test()` 方法中添加调用:
  ```groovy
  testUpdateProvisionNetwork()
  ```

  添加测试方法:
  ```groovy
  void testUpdateProvisionNetwork() {
      def zone = env.inventoryByName("zone") as ZoneInventory

      def net = createProvisionNetwork {
          name = "pxe-update"
          zoneUuid = zone.uuid
          type = "STANDALONE_PXE"
          description = "original desc"
          dhcpInterface = "eth0"
          dhcpRangeStartIp = "10.0.0.100"
          dhcpRangeEndIp = "10.0.0.200"
          dhcpRangeNetmask = "255.255.255.0"
          dhcpRangeGateway = "10.0.0.1"
      } as PhysicalServerProvisionNetworkInventory

      assert net.name == "pxe-update"
      assert net.description == "original desc"

      def updated = updateProvisionNetwork {
          uuid = net.uuid
          name = "pxe-updated"
          description = "updated desc"
          dhcpInterface = "bond0"
          dhcpRangeStartIp = "10.0.1.100"
          dhcpRangeEndIp = "10.0.1.200"
      } as PhysicalServerProvisionNetworkInventory

      assert updated.name == "pxe-updated"
      assert updated.description == "updated desc"
      assert updated.dhcpInterface == "bond0"
      assert updated.dhcpRangeStartIp == "10.0.1.100"
      assert updated.dhcpRangeEndIp == "10.0.1.200"
      // Unchanged fields preserved
      assert updated.dhcpRangeNetmask == "255.255.255.0"
      assert updated.dhcpRangeGateway == "10.0.0.1"
      assert updated.type == "STANDALONE_PXE"

      deleteProvisionNetwork { uuid = net.uuid }
  }
  ```

- [ ] **Step 1.6: 编译验证**

  ```bash
  JAVA_HOME=/usr/lib/jvm/java-8-openjdk-amd64 mvn clean install -pl plugin/physicalServer -am -DskipTests -Dmaven.repo.local=.m2/repository
  ```
  Expected: BUILD SUCCESS

- [ ] **Step 1.7: Commit**

  ```bash
  git add header/src/main/java/org/zstack/header/server/APIUpdateProvisionNetworkMsg.java \
        header/src/main/java/org/zstack/header/server/APIUpdateProvisionNetworkEvent.java \
        plugin/physicalServer/src/main/java/org/zstack/server/PhysicalServerManagerImpl.java \
        conf/serviceConfig/provisionNetwork.xml \
        test/src/test/groovy/org/zstack/test/integration/server/ServerPoolCrudCase.groovy
  git commit -m "feat(server): add APIUpdateProvisionNetworkMsg

  Add missing Update API for ProvisionNetwork CRUD completeness.
  Supports updating name, description, and DHCP fields.

  Change-Id: I$(uuidgen | tr -d '-' | head -c 40)"
  ```

---

### Task P2: oobPassword 添加 @EncryptColumn

**Goal:** 在 PhysicalServerAO.oobPassword 字段添加 @EncryptColumn 注解，确保密码在数据库中加密存储。

**Files to modify:**
- **修改:** `header/src/main/java/org/zstack/header/server/PhysicalServerAO.java` — 添加注解

#### Steps

- [ ] **Step 2.1: 添加 @EncryptColumn 注解**

  在 `PhysicalServerAO.java` 中:

  添加 import:
  ```java
  import org.zstack.header.core.encrypt.EncryptColumn;
  ```

  修改 oobPassword 字段（第 79-80 行）:
  ```java
  @EncryptColumn
  @Column
  private String oobPassword;
  ```

- [ ] **Step 2.2: 编译验证**

  ```bash
  JAVA_HOME=/usr/lib/jvm/java-8-openjdk-amd64 mvn clean install -pl plugin/physicalServer -am -DskipTests -Dmaven.repo.local=.m2/repository
  ```
  Expected: BUILD SUCCESS

- [ ] **Step 2.3: Commit**

  ```bash
  git add header/src/main/java/org/zstack/header/server/PhysicalServerAO.java
  git commit -m "fix(server): add @EncryptColumn to oobPassword

  Ensure OOB password is encrypted at DB level per FR-005.

  Change-Id: I$(uuidgen | tr -d '-' | head -c 40)"
  ```

---

### Task P3: 修复 PhysicalServerState 枚举与测试对齐

**Goal:** 确认 PhysicalServerState 枚举值与测试用例中的断言一致。当前枚举值是 `Maintenance` 但部分测试期望 `Maintaining`。

**Files to check/modify:**
- **检查:** `header/src/main/java/org/zstack/header/server/PhysicalServerState.java`
- **可能修改:** 测试文件中的 `Maintaining` → `Maintenance` 或反之

#### Steps

- [ ] **Step 3.1: 检查枚举定义与测试断言**

  查看 PhysicalServerState.java 中的枚举值。
  如果枚举是 `Maintenance`，那么测试中 `assert maintained.state == "Maintaining"` 会失败。

  **修复方式二选一:**
  - A) 枚举改为 `Maintaining` — 语义更贴合 "正在维护中"
  - B) 测试改为 `"Maintenance"` — 保持枚举不变

  按 ZStack 惯例（HostState.Maintenance），选择 **B: 测试对齐到枚举值 `Maintenance`**。

- [ ] **Step 3.2: 修复测试文件中的断言**

  在 `ServerPoolCrudCase.groovy` 的 `testChangePhysicalServerState()` 中:
  ```groovy
  // 修改前:
  assert maintained.state == "Maintaining"
  // 修改后:
  assert maintained.state == "Maintenance"
  ```

  在 `PhysicalServerCapacityCase.groovy` 的 `testChangeStateMaintainThenEnable()` 中:
  ```groovy
  // 修改前:
  assert maintained.state == "Maintaining"
  // 修改后:
  assert maintained.state == "Maintenance"
  ```

  在 `PhysicalServerCapacityCase.groovy` 的 `testMaintenanceServerExcludedFromAllocation()` 中:
  ```groovy
  // 修改前:
  assert maintained.state == "Maintaining"
  // 修改后:
  assert maintained.state == "Maintenance"

  // 修改前:
  conditions = ["uuid=${server.uuid}".toString(), "state=Maintaining"]
  // 修改后:
  conditions = ["uuid=${server.uuid}".toString(), "state=Maintenance"]
  ```

- [ ] **Step 3.3: Commit**

  ```bash
  git add test/src/test/groovy/org/zstack/test/integration/server/ServerPoolCrudCase.groovy \
        test/src/test/groovy/org/zstack/test/integration/server/PhysicalServerCapacityCase.groovy
  git commit -m "fix(test): align state assertion to Maintenance enum

  PhysicalServerState enum uses 'Maintenance' not 'Maintaining'.
  Fix all test assertions to match the actual enum value.

  Change-Id: I$(uuidgen | tr -d '-' | head -c 40)"
  ```

---

## Post-patch: SDK 重新生成

在 3 个补丁任务完成后，需要重新生成 SDK 以包含 APIUpdateProvisionNetworkMsg:

```bash
JAVA_HOME=/usr/lib/jvm/java-8-openjdk-amd64 mvn -Dmaven.test.skip=true -P premium clean install
JAVA_HOME=/usr/lib/jvm/java-8-openjdk-amd64 ./runMavenProfile sdk
JAVA_HOME=/usr/lib/jvm/java-8-openjdk-amd64 ./runMavenProfile apihelper
```

---

## 自查

| 检查项 | 结果 |
|---|---|
| Spec 覆盖 | ✅ 3 个缺口全部有对应 Task |
| Placeholder scan | ✅ 无 TBD/TODO |
| 类型一致性 | ✅ APIUpdateProvisionNetworkMsg 签名与 APIUpdateServerPoolMsg 模式一致 |
| PhysicalServerState 枚举值 | ✅ 需在 Step 3.1 确认实际值再修复 |
